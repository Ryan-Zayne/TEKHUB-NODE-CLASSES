export const name = 'Zayne';
export const age = 24;
export const country = 'Nigeria';

export function add(x, y) {
	return x + y;
}

export function subtract(x, y) {
	return x - y;
}

export const multiply = (x, y) => x * y;
